# Vino Putec Environment Variables

# --- Stripe ---
# Stripe mode is automatically detected from key prefix:
# - sk_test_* = TEST mode (sandbox)
# - sk_live_* = LIVE mode (production)
# For test mode, use keys starting with pk_test_ and sk_test_
# For production, use keys starting with pk_live_ and sk_live_
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxx
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxxx
# Note: Webhook secret is different for test vs live mode
# Get test webhook secret from: stripe listen (local) or Stripe Dashboard > Webhooks (production)

# --- Resend (emaily) ---
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxx
RESEND_FROM_EMAIL=onboarding@resend.dev
ADMIN_EMAIL=vas-email@domena.sk

# --- SuperFaktura (fakturácia) ---
# SUPERFAKTURA_SANDBOX accepts '1', 'true', 'True', 'TRUE' (case-insensitive)
# Set to '1' or 'true' for SANDBOX mode (testing)
# Set to '0', 'false', or leave empty for PRODUCTION mode
SUPERFAKTURA_EMAIL=vas-email@domena.sk
SUPERFAKTURA_API_KEY=vasklucodsuperFaktury
SUPERFAKTURA_SEND_EMAILS=1
SUPERFAKTURA_SANDBOX=1
# Note: Use sandbox credentials when SUPERFAKTURA_SANDBOX=1
# Use production credentials when SUPERFAKTURA_SANDBOX=0 or not set

# --- Optional: Google reCAPTCHA v3 (if enabled) ---
# NEXT_PUBLIC_RECAPTCHA_SITE_KEY=xxxxxxxxxxxxxxxxxxxxxxxxx
# RECAPTCHA_SECRET_KEY=xxxxxxxxxxxxxxxxxxxxxxxxx

# NOTE:
# - Do NOT commit your real .env.local. This env.example is a template only.
# - For local development, create .env.local with your real keys.
# - In production (Vercel), set the same keys in Project Settings → Environment Variables.

NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=